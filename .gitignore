*.o
.env
.claude/
.tokensave
.serena/
.venv/
__pycache__/

# Zig package dependencies
.zig-pkg/
zig-pkg/

# Zig build artifacts and caches
.zig-cache/
zig-cache/
zig-out/

# ROMs are never committed; the free test ROMs are fetched by
# tools/fetch_test_roms.sh.
roms/

# Save states and SRAM saves written next to a ROM.
*.state
*.srm

# Nuked-OPN2, the FM reference, fetched by tools/fetch_ym_reference.sh.
testdata/

# Headless --shot output.
*.png
