*.o
.env
.claude/
.tokensave
.serena/
.venv/
__pycache__/

# Zig package dependencies
.zig-pkg/
zig-pkg/

# Zig build artifacts and caches
.zig-cache/
zig-cache/
zig-out/

# ROMs: commercial ROMs (Sonic-the-Hedgehog.bin, etc.) are never committed,
# and hardware test ROMs are fetched by tools/fetch_test_roms.sh.
roms/

# Save states and SRAM saves written next to a ROM.
*.state
*.srm
